<?xml version="1.0" encoding="UTF-8"?>
<schemalist>
  <schema id="org.gnome.shell.extensions.fix-panel-order-by-blueray453"
          path="/org/gnome/shell/extensions/fix-panel-order-by-blueray453/">

    <!-- User's desired order. Roles listed here get repositioned;
         roles present in the box but not listed are left where they
         are (matches the original safelyReorder behavior). -->
    <key name="order-left" type="as">
      <default>['workspace-indicator']</default>
      <summary>Desired order of indicators in the left panel box</summary>
    </key>
    <key name="order-center" type="as">
      <default>[]</default>
      <summary>Desired order of indicators in the center panel box</summary>
    </key>
    <key name="order-right" type="as">
      <default>['ShowNetSpeedButton', 'printers', 'lockkeys@febueldo.test', 'color-picker@tuberry', 'clipboardIndicator', 'emoji-copy@felipeftn', 'athan@goodm4ven']</default>
      <summary>Desired order of indicators in the right panel box</summary>
    </key>

    <!-- Extension-managed, read-only from prefs' side: a live snapshot
         of which roles are actually present in each box right now, in
         their current on-screen order. -->
    <key name="discovered-left" type="as">
      <default>[]</default>
      <summary>Roles currently present in the left panel box</summary>
    </key>
    <key name="discovered-center" type="as">
      <default>[]</default>
      <summary>Roles currently present in the center panel box</summary>
    </key>
    <key name="discovered-right" type="as">
      <default>[]</default>
      <summary>Roles currently present in the right panel box</summary>
    </key>

  </schema>
</schemalist>